.env
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# Local terminal state (never committed â€” see architecture Â§14)
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
pos.lock

# UI build output is generated into the Python package
app/ui/
ui-src/node_modules/
# Vite writes one of these per run and does not always clean up after itself.
ui-src/vitest.config.ts.timestamp-*
ui-src/dist/

# TypeScript build cache
ui-src/*.tsbuildinfo

# Supabase CLI scratch state (link details, cached versions)
supabase/.temp/

# Database dumps (never committed â€” real catalogue data)
backup-*

# Test and check output, from piping a run through Tee-Object. Never worth
# committing, and PowerShell writes them as UTF-16 which reads as binary in a
# diff.
*.log
Claude outputs/

# Commit message drafts, written by Claude and consumed by `git commit -F`.
commit-message.txt
